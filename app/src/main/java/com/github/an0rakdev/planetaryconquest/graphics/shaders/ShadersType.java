package com.github.an0rakdev.planetaryconquest.graphics.shaders;

/**
 * The basic shaders types.
 *
 * @author Sylvain Nieuwlandt
 * @version 1.0
 */
public enum ShadersType {
	/**
	 * The vertex shader type (for vertices positions).
	 */
	VERTEX,
	/**
	 * The fragment shader type (for color mapping).
	 */
	FRAGMENT;
}
